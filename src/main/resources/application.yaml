server:
  port: 8080
  servlet:
    context-path: /identity
spring:
  datasource:
    url: "jdbc:mysql://localhost:3307/identityService"
    username: root
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true